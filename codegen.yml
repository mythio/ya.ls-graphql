overwrite: true
schema: "./src/graphql/type-defs.graphql"
config:
  skipTypename: true
  typesPrefix: I
documents: null
generates:
  src/types/schemaType.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"