const bcrypt = require('bcrypt');

const { User }= require('../models/user');

module.exports = {
  createUser: async function ({ userInput }) {
    const errors = [];
    const user = new User
    if (!)
  });
}